provider:
  name: openfaas
  gateway: https://phxlvveba01.itplab.local
functions:
  testfunction:
    lang: powercli
    handler: ./handler
    image: cmstraka/veba-base:latest
    environment:
      skip_nsx_cert_check: true
      prod_environment: false
    secrets:
      - nsx-secrets
    annotations:
      topic: com.vmware.cis.tagging.attach
